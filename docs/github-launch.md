# GitHub launch settings

Recommended repository:

- **Name:** `lee-relay`
- **Visibility:** Public
- **License:** MIT
- **Default branch:** `main`
- **Description:** `Browser-native multi-AI meeting room for ChatGPT, Claude, Gemini & Copilot — no API keys, no separate relay server.`

Recommended topics:

`multi-agent`, `ai-agents`, `chrome-extension`, `browser-automation`, `chatgpt`, `claude`, `gemini`, `copilot`, `no-api-key`, `multi-ai`, `side-panel`, `ai-tools`

Recommended About text:

> Online AI. Local orchestration. No API middleman.

## Launch checklist

1. Create the public `lee-relay` repository without auto-generating README/license files.
2. Push this repository-ready folder to `main`.
3. Confirm GitHub Actions CI is green.
4. Create tag `v3.0.1` to trigger the release workflow.
5. Add a sanitized Side Panel screenshot or short GIF to the README after removing account/chat-history data.
6. Configure a private vulnerability reporting channel, then update `SECURITY.md`.
7. Pin the repository on the GitHub profile.
