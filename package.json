{
  "name": "lee-relay",
  "version": "3.0.2",
  "private": true,
  "description": "Browser-native multi-AI meeting room for ChatGPT, Claude, Gemini, and Copilot — no API keys required.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check background.js && node --check content.js && node --check sidepanel.js && node --check relay-core.mjs && node --check meeting-engine.mjs && node --check provider-adapters.mjs && node --check router.mjs && node --check transaction-engine.mjs",
    "package": "node scripts/package-extension.mjs",
    "verify-package": "node scripts/verify-package.mjs",
    "verify": "npm test && npm run check && node scripts/verify-manifest.mjs && npm run package && npm run verify-package"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
